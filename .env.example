# Copy this file to .env and adjust to your needs.
# .env is gitignored and will not be committed.

# Share a host directory with the VM (mounted at /home/claude/shared):
# SHARED_FOLDER="/path/to/your/project"

# TCP ports to forward from guest to host (localhost:<port>):
# FORWARDED_PORTS="3000,3001,3002,6006"
